import PlatformButton from "../components/PlatformButton";

const GLOBAL = {
    TabList: [
        {
            component: <PlatformButton />,
            props: {},
        },
    ],
};

export default GLOBAL;
